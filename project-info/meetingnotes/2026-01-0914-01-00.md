Deliverable 5 Requirements:


We are required to submit the Application code to the supervisor.


This code must offer "real value" and we need to verify with the supervisor what constitutes this.


We must use JUnit for testing.


We need to provide a screenshot of the JUnit file hierarchy and a table explaining what each file tests.


We must reproduce the Design section (UMLs, Sketches, Scenarios) and update them based on any changes.


We need to include a specific "Response to Feedback" chapter/page.


We need to provide a complete burndown for D5, an initial burndown for D6, and an updated sprint plan for D4 with actual hours.

Issues raised in D3 Feedback:
Our scope was noted as too broad. The feedback suggested picking one or two key features (e.g. just the calculator or reminders) rather than attempting everything.
User stories in D3 focused too much on non-functional requirements.
Prototypes were inconsistent and lacked a common layout.
The homepage needs to be redesigned to show a common use case rather than just buttons.

Issues raised in D4 Feedback:
Requirements Planning:
Several user stories are too large, specifically the 20 and 40-point items.
These large stories bundle multiple concerns which reduces independence.
Some stories include implicit design decisions (interface behaviours or content presentation) rather than focusing purely on user goals.

Design:
Sketches are missing annotations or contain only short labels.
We need to add brief notes explaining what users can do on each screen and how they move to the next one.
Scenarios currently describe a generic role such as "user".
We need to rewrite each scenario so that it is clearly tied to one specific persona with a concrete goal.

Project Planning:
Integration is not mentioned within the planning section.
We need to add integration directly into the sprint plan with its own estimation and assigned member.
Integration must NOT be placed in the burndown chart, as the chart must focus only on user story effort.

Project Set-up:
The risk management plan focuses primarily on mitigation and prevention.
We need to make contingency actions (fallback/recovery) more explicit for when risks materialise despite mitigation.

We need to discuss the following after exams:
Deciding which features to cut to address the scope feedback.
Setting up the JUnit testing environment.
Updating the User Stories to be smaller and independent.
Annotating sketches and rewriting scenarios.
Updating the Risk Register and Sprint Plans.

